doctype html

html
	head
		title 吧唧

		link(rel='stylesheet' href='/common.css')
		link(rel='stylesheet' href='/home.css')
	body
		include header.pug
		|
		|
		if(user)
			p 欢迎，#{user.name}
				a(href='/logout') 退出账号
		else
			p 未登录
				a(href="/login") 点击登录
				br
				|没有账号？
				a(href="/register") 点击注册
		
		a.new-post(href='/post') 我要发贴！
	
		ul
			each post in posts
				li.post-item
					a(href=`/user/${post.user.id}`)
						img(src=post.user.avatar alt=post.user.name)
					h2
						a(href=`/post/${post.id}`)=post.title
					span 发帖时间：#{new Date(post.createdAt).toString().substr(0,24)}
					span 回帖数量： #{post.commentCount}

		include footer.pug